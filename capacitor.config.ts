import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ds.cosmicds.solareclipse2024',
  appName: 'solar-eclipse-2024',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
